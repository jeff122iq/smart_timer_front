import { makeStyles, Theme, createStyles } from "@material-ui/core";

export default makeStyles((theme: Theme) =>
  createStyles({
    container: {
      width: "100%",
      margin: "0 32px",
    },
  })
);
